# greenpay-gatsby-frontend

A simple frontend for a South African payment site (original logo changed for client privacy).

Uses Gatsby for a statically built frontpage, and GraphQL to query a Contentful CMS.
